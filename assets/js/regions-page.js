const regionPageData=getEstateData();document.querySelector('#regionPageGrid').innerHTML=regionPageData.regions.map(r=>`<a class="region-page-card" href="satilik.html?region=${encodeURIComponent(r.name)}"><img src="${r.image}" alt="${r.name}"><div><h2>${r.name}</h2><span>${r.province} · ${regionPageData.listings.filter(x=>x.region===r.name).length} İLAN →</span></div></a>`).join('');
