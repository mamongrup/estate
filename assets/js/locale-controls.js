(function(){const language=document.querySelector('#language'),currency=document.querySelector('#currency'),languageLabel=document.querySelector('#languageLabel'),currencyLabel=document.querySelector('#currencyLabel');function sync(select,label){if(select&&label)label.textContent=select.options[select.selectedIndex]?.textContent||select.value}language?.addEventListener('change',()=>sync(language,languageLabel));currency?.addEventListener('change',()=>sync(currency,currencyLabel));sync(language,languageLabel);sync(currency,currencyLabel)})();
